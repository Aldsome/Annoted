<Window x:Class="Annoted.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Annoted.Wpf.Views"
        xmlns:vm="clr-namespace:Annoted.Wpf.ViewModels"
        xmlns:local="clr-namespace:Annoted.Wpf"
        Title="{Binding WindowTitle}"
        Width="1100" Height="760"
        MinWidth="640" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForeBrush}">

    <Window.Resources>
        <!-- Converters are declared app-wide in App.xaml -->

        <!-- Flat tab control -->
        <Style x:Key="FlatTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <Style x:Key="FlatTabItem" TargetType="TabItem">
            <Setter Property="Background" Value="{DynamicResource TabInactiveBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForeBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderThickness="0,2,0,0" BorderBrush="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <TextBlock Text="{TemplateBinding Header}"
                                       Foreground="{TemplateBinding Foreground}"
                                       TextTrimming="CharacterEllipsis" MaxWidth="160"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource TabActiveBrush}"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ButtonHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- EditorStyle is declared app-wide in App.xaml (used by EditorView UserControl) -->
    </Window.Resources>

    <!-- Hotkeys are wired in code-behind (BuildHotkeys) — KeyBinding.Command bindings
         can't resolve the DataContext because InputBindings aren't in the logical tree. -->

    <DockPanel>
        <!-- Menu -->
        <Menu DockPanel.Dock="Top"
              Background="{DynamicResource SurfaceBrush}"
              Foreground="{DynamicResource ForeBrush}">
            <MenuItem Header="_File">
                <MenuItem Header="_New Tab"       Command="{Binding NewTabCommand}"        InputGestureText="Ctrl+N"/>
                <MenuItem Header="_Open..."       Command="{Binding OpenDocumentCommand}"  InputGestureText="Ctrl+O"/>
                <MenuItem Header="_Save"          Command="{Binding SaveDocumentCommand}"  InputGestureText="Ctrl+S"/>
                <MenuItem Header="Save _As..."    Command="{Binding SaveDocumentAsCommand}" InputGestureText="Ctrl+Shift+S"/>
                <MenuItem Header="_Close Tab"     Command="{Binding CloseTabCommand}"      InputGestureText="Ctrl+W"/>
                <Separator/>
                <MenuItem Header="Export Tab As _Image..." Command="{Binding ExportAsImageCommand}"/>
                <MenuItem Header="_Print..."      Command="{Binding PrintCommand}"         InputGestureText="Ctrl+P"/>
                <Separator/>
                <MenuItem Header="Storage _Location..." Command="{Binding ChooseStorageLocationCommand}"/>
                <Separator/>
                <MenuItem Header="E_xit" Click="Exit_Click" InputGestureText="Alt+F4"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo"         Command="{Binding UndoCommand}"         InputGestureText="Ctrl+Z"/>
                <MenuItem Header="_Redo"         Command="{Binding RedoCommand}"         InputGestureText="Ctrl+Y"/>
                <Separator/>
                <MenuItem Header="Cu_t"          Command="Cut"                           InputGestureText="Ctrl+X"/>
                <MenuItem Header="_Copy"         Command="Copy"                          InputGestureText="Ctrl+C"/>
                <MenuItem Header="_Paste"        Command="Paste"                         InputGestureText="Ctrl+V"/>
                <MenuItem Header="Select _All"   Command="SelectAll"                     InputGestureText="Ctrl+A"/>
                <Separator/>
                <MenuItem Header="_Find..."      Command="{Binding FindCommand}"         InputGestureText="Ctrl+F"/>
                <MenuItem Header="Find _Next"    Command="{Binding FindNextCommand}"     InputGestureText="F3"/>
                <MenuItem Header="Find _Previous" Command="{Binding FindPreviousCommand}" InputGestureText="Shift+F3"/>
                <MenuItem Header="_Replace..."   Command="{Binding ReplaceCommand}"      InputGestureText="Ctrl+H"/>
                <MenuItem Header="_Go To..."     Command="{Binding GoToLineCommand}"     InputGestureText="Ctrl+G"/>
                <MenuItem Header="Time/_Date"    Command="{Binding InsertDateTimeCommand}" InputGestureText="F5"/>
            </MenuItem>
            <MenuItem Header="F_ormat">
                <MenuItem Header="_Word Wrap"    Command="{Binding ToggleWordWrapCommand}"
                          IsCheckable="True" IsChecked="{Binding IsWordWrap}"/>
                <MenuItem Header="_Font..."      Command="{Binding ChooseFontCommand}"/>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="Zoom _In"      Command="{Binding ZoomInCommand}"       InputGestureText="Ctrl++"/>
                <MenuItem Header="Zoom _Out"     Command="{Binding ZoomOutCommand}"      InputGestureText="Ctrl+-"/>
                <MenuItem Header="_Reset Zoom"   Command="{Binding ResetZoomCommand}"    InputGestureText="Ctrl+0"/>
                <Separator/>
                <MenuItem Header="_Word Count"   Command="{Binding ShowWordCountCommand}"/>
            </MenuItem>
            <MenuItem Header="_Theme">
                <MenuItem Header="_Dark Mode"
                          IsCheckable="True" IsChecked="{Binding IsDarkMode, Mode=TwoWay}"/>
                <Separator/>
                <MenuItem Header="_Appearance..." Command="{Binding ShowAppearanceCommand}"/>
            </MenuItem>
            <MenuItem Header="_Audio">
                <MenuItem Header="_Record"                Command="{Binding RecordAudioCommand}"/>
                <MenuItem Header="_Stop Recording"        Command="{Binding StopAudioCommand}"/>
                <MenuItem Header="_Play Recording"        Command="{Binding PlayAudioCommand}"/>
                <Separator/>
                <MenuItem Header="Speech _Model…"         Command="{Binding ChangeWhisperModelCommand}"/>
                <MenuItem Header="_Dictate (Speech to Text)" Command="{Binding ToggleDictationMenuCommand}"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About Annoted" Command="{Binding ShowAboutCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Status bar -->
        <StatusBar DockPanel.Dock="Bottom"
                   Background="{DynamicResource StatusBarBackBrush}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="11"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Background download hint bar (non-blocking) -->
        <Border DockPanel.Dock="Bottom"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0"
                Padding="12,6"
                Visibility="{Binding AudioSidebar.DownloadActive, Converter={StaticResource BoolToVisibility}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="11"
                           Text="{Binding AudioSidebar.DownloadStatus}"
                           TextTrimming="CharacterEllipsis"
                           Foreground="{DynamicResource ForeBrush}"/>
                <ProgressBar Grid.Column="1" Height="8" Margin="8,0" VerticalAlignment="Center"
                             Minimum="0" Maximum="100" Foreground="{DynamicResource AccentBrush}"
                             Value="{Binding AudioSidebar.DownloadPercent}"/>
                <Button Grid.Column="2" Content="Cancel" FontSize="11" Padding="8,2"
                        Click="CancelDownload_Click"/>
            </Grid>
        </Border>

        <!-- Main content: tabs + sidebar -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Tab control -->
            <TabControl Grid.Column="0"
                        Style="{StaticResource FlatTabControl}"
                        ItemsSource="{Binding Tabs}"
                        SelectedItem="{Binding ActiveTab}">
                <TabControl.ItemContainerStyle>
                    <Style TargetType="TabItem" BasedOn="{StaticResource FlatTabItem}">
                        <Setter Property="Header" Value="{Binding Title}"/>
                        <Setter Property="Content" Value="{Binding}"/>
                    </Style>
                </TabControl.ItemContainerStyle>
                <TabControl.ContentTemplate>
                    <DataTemplate DataType="{x:Type vm:TabViewModel}">
                        <views:EditorView DataContext="{Binding}"/>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>

            <!-- Audio sidebar -->
            <views:AudioSidebarView Grid.Column="1"
                                    DataContext="{Binding AudioSidebar}"/>
        </Grid>
    </DockPanel>
</Window>
